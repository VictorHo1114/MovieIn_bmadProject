========================================
 部署問題完整診斷
========================================

當前狀態:
-  前端仍在連接 127.0.0.1:8000 (本地)
-  本地 .env.production 設置正確
-  Vercel 環境變數狀態未知

========================================
解決方案 A: Vercel 環境變數檢查
========================================

請確認以下步驟都已完成:

 1. 登入 Vercel Dashboard (https://vercel.com)
 2. 選擇正確的專案
 3. Settings  Environment Variables
 4. 確認已添加:
     - NEXT_PUBLIC_API_BASE = https://moviein-api.onrender.com/api/v1
     - NEXT_PUBLIC_USE_MOCKS = false
 5. 環境選擇了 Production, Preview, Development (全部勾選)
 6. 已保存變數
 7. 已重新部署 (Deployments  Redeploy)
 8. 等待部署完成 (狀態顯示 Ready)

========================================
解決方案 B: 檢查部署日誌
========================================

在 Vercel Dashboard:
1. Deployments  點擊最新部署
2. 點擊 'Building' 或 'Function Logs'
3. 查找環境變數相關訊息
4. 確認 NEXT_PUBLIC_API_BASE 有被讀取

========================================
解決方案 C: 強制使用生產配置
========================================

如果環境變數仍未生效，我們可以修改代碼強制使用生產 URL:

修改 frontend/lib/config.ts:
-----------------------------------------
export const API_BASE =
  process.env.NEXT_PUBLIC_API_BASE ?? 
  (process.env.NODE_ENV === 'production' 
    ? 'https://moviein-api.onrender.com/api/v1'
    : 'http://127.0.0.1:8000/api/v1');
-----------------------------------------

這樣即使環境變數沒設置，生產環境也會使用正確的 URL。

========================================
解決方案 D: 使用 Vercel CLI 驗證
========================================

在本地終端執行:
1. vercel env ls
   (查看所有環境變數)

2. vercel env pull .env.vercel
   (下載 Vercel 的環境變數)

3. 查看 .env.vercel 文件確認變數是否存在

========================================
立即行動建議
========================================

請告訴我:
1. 你是否已在 Vercel Dashboard 添加環境變數?
    是   否   不確定

2. 添加後是否已重新部署?
    是   否

3. 你想要:
    A) 我再次確認 Vercel 設置
    B) 修改代碼強制使用生產 URL (最快)
    C) 使用 Vercel CLI 檢查

========================================
快速修復 (推薦)
========================================

如果想最快解決，選擇方案 C:
我會幫你修改 config.ts，讓生產環境自動使用正確的 URL，
不依賴環境變數。

要我現在幫你做這個修改嗎? (Y/N)

========================================
